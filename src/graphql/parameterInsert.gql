mutation(
  $executorId: String!
  $missionDefId: String!
  $name: String!
  $type: String!
  $required: Boolean!
  $defaultValue: String
  $description: String
) {
  createParameter(
    input: {
      parameter: {
        executorId: $executorId
        missionDefId: $missionDefId
        description: $description
        name: $name
        type: $type
        defaultValue: $defaultValue
        required: $required
      }
    }
  ) {
    parameter {
      executorId
      missionDefId
      description
      name
      type
      required
    }
  }
}
