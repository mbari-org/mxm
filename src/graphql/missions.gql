query missions($executorId: String!) {
  allMissionsList(condition: {
    executorId: $executorId
  }) {
    id
    executorId
    missionId
    missionDefId
    assetId
    name
    description
    startDate
    endDate
    geometry
  }
}
