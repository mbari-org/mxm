query missions($executorId: String!) {
  allMissionsList(condition: {
    executorId: $executorId
  }) {
    id
    executorId
    missionId
    missionDefId
    assetId
    description
    startDate
    endDate
    geometry
  }
}
