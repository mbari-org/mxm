query missions($executorId: String!) {
  allMissionsList(condition: {
    executorId: $executorId
  }) {
    id
    executorId
    missionTplId
    missionId
    missionStatus
    assetId
    description
    startDate
    endDate
  }
}
